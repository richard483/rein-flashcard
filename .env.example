# Karasu Auth base URL for SSR auth flows and external token validation.
PRIVATE_AUTH_BASE_URL=https://auth.nephren.xyz

# PostgreSQL connection settings for the flashcard database.
PRIVATE_DB_HOST=localhost
PRIVATE_DB_PORT=5432
PRIVATE_DB_NAME=rein_flashcard
PRIVATE_DB_USER=postgres
PRIVATE_DB_PASSWORD=postgres
